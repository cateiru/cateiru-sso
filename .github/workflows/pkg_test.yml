name: Package test

on:
  push:
    paths:
      - 'pkg/**'
  pull_request:
    branches: [ main ]

jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.18

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
