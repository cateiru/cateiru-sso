import {Session} from '../../components/Common/Session';
import {OrgList} from '../../components/Organization/OrgList';

const Page = () => {
  return (
    <Session>
      <OrgList />
    </Session>
  );
};

export default Page;
