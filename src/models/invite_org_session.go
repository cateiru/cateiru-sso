// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// InviteOrgSession is an object representing the database table.
type InviteOrgSession struct {
	ID        uint      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Token     string    `boil:"token" json:"token" toml:"token" yaml:"token"`
	Email     string    `boil:"email" json:"email" toml:"email" yaml:"email"`
	Period    time.Time `boil:"period" json:"period" toml:"period" yaml:"period"`
	OrgID     string    `boil:"org_id" json:"org_id" toml:"org_id" yaml:"org_id"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *inviteOrgSessionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L inviteOrgSessionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var InviteOrgSessionColumns = struct {
	ID        string
	Token     string
	Email     string
	Period    string
	OrgID     string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	Token:     "token",
	Email:     "email",
	Period:    "period",
	OrgID:     "org_id",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

var InviteOrgSessionTableColumns = struct {
	ID        string
	Token     string
	Email     string
	Period    string
	OrgID     string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "invite_org_session.id",
	Token:     "invite_org_session.token",
	Email:     "invite_org_session.email",
	Period:    "invite_org_session.period",
	OrgID:     "invite_org_session.org_id",
	CreatedAt: "invite_org_session.created_at",
	UpdatedAt: "invite_org_session.updated_at",
}

// Generated where

var InviteOrgSessionWhere = struct {
	ID        whereHelperuint
	Token     whereHelperstring
	Email     whereHelperstring
	Period    whereHelpertime_Time
	OrgID     whereHelperstring
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperuint{field: "`invite_org_session`.`id`"},
	Token:     whereHelperstring{field: "`invite_org_session`.`token`"},
	Email:     whereHelperstring{field: "`invite_org_session`.`email`"},
	Period:    whereHelpertime_Time{field: "`invite_org_session`.`period`"},
	OrgID:     whereHelperstring{field: "`invite_org_session`.`org_id`"},
	CreatedAt: whereHelpertime_Time{field: "`invite_org_session`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`invite_org_session`.`updated_at`"},
}

// InviteOrgSessionRels is where relationship names are stored.
var InviteOrgSessionRels = struct {
	Org string
}{
	Org: "Org",
}

// inviteOrgSessionR is where relationships are stored.
type inviteOrgSessionR struct {
	Org *Organization `boil:"Org" json:"Org" toml:"Org" yaml:"Org"`
}

// NewStruct creates a new relationship struct
func (*inviteOrgSessionR) NewStruct() *inviteOrgSessionR {
	return &inviteOrgSessionR{}
}

func (r *inviteOrgSessionR) GetOrg() *Organization {
	if r == nil {
		return nil
	}
	return r.Org
}

// inviteOrgSessionL is where Load methods for each relationship are stored.
type inviteOrgSessionL struct{}

var (
	inviteOrgSessionAllColumns            = []string{"id", "token", "email", "period", "org_id", "created_at", "updated_at"}
	inviteOrgSessionColumnsWithoutDefault = []string{"token", "email", "org_id"}
	inviteOrgSessionColumnsWithDefault    = []string{"id", "period", "created_at", "updated_at"}
	inviteOrgSessionPrimaryKeyColumns     = []string{"id"}
	inviteOrgSessionGeneratedColumns      = []string{}
)

type (
	// InviteOrgSessionSlice is an alias for a slice of pointers to InviteOrgSession.
	// This should almost always be used instead of []InviteOrgSession.
	InviteOrgSessionSlice []*InviteOrgSession
	// InviteOrgSessionHook is the signature for custom InviteOrgSession hook methods
	InviteOrgSessionHook func(context.Context, boil.ContextExecutor, *InviteOrgSession) error

	inviteOrgSessionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	inviteOrgSessionType                 = reflect.TypeOf(&InviteOrgSession{})
	inviteOrgSessionMapping              = queries.MakeStructMapping(inviteOrgSessionType)
	inviteOrgSessionPrimaryKeyMapping, _ = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, inviteOrgSessionPrimaryKeyColumns)
	inviteOrgSessionInsertCacheMut       sync.RWMutex
	inviteOrgSessionInsertCache          = make(map[string]insertCache)
	inviteOrgSessionUpdateCacheMut       sync.RWMutex
	inviteOrgSessionUpdateCache          = make(map[string]updateCache)
	inviteOrgSessionUpsertCacheMut       sync.RWMutex
	inviteOrgSessionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var inviteOrgSessionAfterSelectHooks []InviteOrgSessionHook

var inviteOrgSessionBeforeInsertHooks []InviteOrgSessionHook
var inviteOrgSessionAfterInsertHooks []InviteOrgSessionHook

var inviteOrgSessionBeforeUpdateHooks []InviteOrgSessionHook
var inviteOrgSessionAfterUpdateHooks []InviteOrgSessionHook

var inviteOrgSessionBeforeDeleteHooks []InviteOrgSessionHook
var inviteOrgSessionAfterDeleteHooks []InviteOrgSessionHook

var inviteOrgSessionBeforeUpsertHooks []InviteOrgSessionHook
var inviteOrgSessionAfterUpsertHooks []InviteOrgSessionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *InviteOrgSession) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *InviteOrgSession) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *InviteOrgSession) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *InviteOrgSession) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *InviteOrgSession) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *InviteOrgSession) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *InviteOrgSession) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *InviteOrgSession) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *InviteOrgSession) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inviteOrgSessionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddInviteOrgSessionHook registers your hook function for all future operations.
func AddInviteOrgSessionHook(hookPoint boil.HookPoint, inviteOrgSessionHook InviteOrgSessionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		inviteOrgSessionAfterSelectHooks = append(inviteOrgSessionAfterSelectHooks, inviteOrgSessionHook)
	case boil.BeforeInsertHook:
		inviteOrgSessionBeforeInsertHooks = append(inviteOrgSessionBeforeInsertHooks, inviteOrgSessionHook)
	case boil.AfterInsertHook:
		inviteOrgSessionAfterInsertHooks = append(inviteOrgSessionAfterInsertHooks, inviteOrgSessionHook)
	case boil.BeforeUpdateHook:
		inviteOrgSessionBeforeUpdateHooks = append(inviteOrgSessionBeforeUpdateHooks, inviteOrgSessionHook)
	case boil.AfterUpdateHook:
		inviteOrgSessionAfterUpdateHooks = append(inviteOrgSessionAfterUpdateHooks, inviteOrgSessionHook)
	case boil.BeforeDeleteHook:
		inviteOrgSessionBeforeDeleteHooks = append(inviteOrgSessionBeforeDeleteHooks, inviteOrgSessionHook)
	case boil.AfterDeleteHook:
		inviteOrgSessionAfterDeleteHooks = append(inviteOrgSessionAfterDeleteHooks, inviteOrgSessionHook)
	case boil.BeforeUpsertHook:
		inviteOrgSessionBeforeUpsertHooks = append(inviteOrgSessionBeforeUpsertHooks, inviteOrgSessionHook)
	case boil.AfterUpsertHook:
		inviteOrgSessionAfterUpsertHooks = append(inviteOrgSessionAfterUpsertHooks, inviteOrgSessionHook)
	}
}

// One returns a single inviteOrgSession record from the query.
func (q inviteOrgSessionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*InviteOrgSession, error) {
	o := &InviteOrgSession{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for invite_org_session")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all InviteOrgSession records from the query.
func (q inviteOrgSessionQuery) All(ctx context.Context, exec boil.ContextExecutor) (InviteOrgSessionSlice, error) {
	var o []*InviteOrgSession

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to InviteOrgSession slice")
	}

	if len(inviteOrgSessionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all InviteOrgSession records in the query.
func (q inviteOrgSessionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count invite_org_session rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q inviteOrgSessionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if invite_org_session exists")
	}

	return count > 0, nil
}

// Org pointed to by the foreign key.
func (o *InviteOrgSession) Org(mods ...qm.QueryMod) organizationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.OrgID),
	}

	queryMods = append(queryMods, mods...)

	return Organizations(queryMods...)
}

// LoadOrg allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (inviteOrgSessionL) LoadOrg(ctx context.Context, e boil.ContextExecutor, singular bool, maybeInviteOrgSession interface{}, mods queries.Applicator) error {
	var slice []*InviteOrgSession
	var object *InviteOrgSession

	if singular {
		var ok bool
		object, ok = maybeInviteOrgSession.(*InviteOrgSession)
		if !ok {
			object = new(InviteOrgSession)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeInviteOrgSession)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeInviteOrgSession))
			}
		}
	} else {
		s, ok := maybeInviteOrgSession.(*[]*InviteOrgSession)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeInviteOrgSession)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeInviteOrgSession))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &inviteOrgSessionR{}
		}
		args = append(args, object.OrgID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &inviteOrgSessionR{}
			}

			for _, a := range args {
				if a == obj.OrgID {
					continue Outer
				}
			}

			args = append(args, obj.OrgID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`organization`),
		qm.WhereIn(`organization.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Organization")
	}

	var resultSlice []*Organization
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Organization")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for organization")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for organization")
	}

	if len(organizationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Org = foreign
		if foreign.R == nil {
			foreign.R = &organizationR{}
		}
		foreign.R.OrgInviteOrgSessions = append(foreign.R.OrgInviteOrgSessions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OrgID == foreign.ID {
				local.R.Org = foreign
				if foreign.R == nil {
					foreign.R = &organizationR{}
				}
				foreign.R.OrgInviteOrgSessions = append(foreign.R.OrgInviteOrgSessions, local)
				break
			}
		}
	}

	return nil
}

// SetOrg of the inviteOrgSession to the related item.
// Sets o.R.Org to related.
// Adds o to related.R.OrgInviteOrgSessions.
func (o *InviteOrgSession) SetOrg(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Organization) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `invite_org_session` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"org_id"}),
		strmangle.WhereClause("`", "`", 0, inviteOrgSessionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OrgID = related.ID
	if o.R == nil {
		o.R = &inviteOrgSessionR{
			Org: related,
		}
	} else {
		o.R.Org = related
	}

	if related.R == nil {
		related.R = &organizationR{
			OrgInviteOrgSessions: InviteOrgSessionSlice{o},
		}
	} else {
		related.R.OrgInviteOrgSessions = append(related.R.OrgInviteOrgSessions, o)
	}

	return nil
}

// InviteOrgSessions retrieves all the records using an executor.
func InviteOrgSessions(mods ...qm.QueryMod) inviteOrgSessionQuery {
	mods = append(mods, qm.From("`invite_org_session`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`invite_org_session`.*"})
	}

	return inviteOrgSessionQuery{q}
}

// FindInviteOrgSession retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindInviteOrgSession(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*InviteOrgSession, error) {
	inviteOrgSessionObj := &InviteOrgSession{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `invite_org_session` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, inviteOrgSessionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from invite_org_session")
	}

	if err = inviteOrgSessionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return inviteOrgSessionObj, err
	}

	return inviteOrgSessionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *InviteOrgSession) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no invite_org_session provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(inviteOrgSessionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	inviteOrgSessionInsertCacheMut.RLock()
	cache, cached := inviteOrgSessionInsertCache[key]
	inviteOrgSessionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			inviteOrgSessionAllColumns,
			inviteOrgSessionColumnsWithDefault,
			inviteOrgSessionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `invite_org_session` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `invite_org_session` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `invite_org_session` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, inviteOrgSessionPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into invite_org_session")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == inviteOrgSessionMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for invite_org_session")
	}

CacheNoHooks:
	if !cached {
		inviteOrgSessionInsertCacheMut.Lock()
		inviteOrgSessionInsertCache[key] = cache
		inviteOrgSessionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the InviteOrgSession.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *InviteOrgSession) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	inviteOrgSessionUpdateCacheMut.RLock()
	cache, cached := inviteOrgSessionUpdateCache[key]
	inviteOrgSessionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			inviteOrgSessionAllColumns,
			inviteOrgSessionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update invite_org_session, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `invite_org_session` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, inviteOrgSessionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, append(wl, inviteOrgSessionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update invite_org_session row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for invite_org_session")
	}

	if !cached {
		inviteOrgSessionUpdateCacheMut.Lock()
		inviteOrgSessionUpdateCache[key] = cache
		inviteOrgSessionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q inviteOrgSessionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for invite_org_session")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for invite_org_session")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o InviteOrgSessionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inviteOrgSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `invite_org_session` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, inviteOrgSessionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in inviteOrgSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all inviteOrgSession")
	}
	return rowsAff, nil
}

var mySQLInviteOrgSessionUniqueColumns = []string{
	"id",
	"token",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *InviteOrgSession) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no invite_org_session provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(inviteOrgSessionColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLInviteOrgSessionUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	inviteOrgSessionUpsertCacheMut.RLock()
	cache, cached := inviteOrgSessionUpsertCache[key]
	inviteOrgSessionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			inviteOrgSessionAllColumns,
			inviteOrgSessionColumnsWithDefault,
			inviteOrgSessionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			inviteOrgSessionAllColumns,
			inviteOrgSessionPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert invite_org_session, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`invite_org_session`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `invite_org_session` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for invite_org_session")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == inviteOrgSessionMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(inviteOrgSessionType, inviteOrgSessionMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for invite_org_session")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for invite_org_session")
	}

CacheNoHooks:
	if !cached {
		inviteOrgSessionUpsertCacheMut.Lock()
		inviteOrgSessionUpsertCache[key] = cache
		inviteOrgSessionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single InviteOrgSession record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *InviteOrgSession) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no InviteOrgSession provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), inviteOrgSessionPrimaryKeyMapping)
	sql := "DELETE FROM `invite_org_session` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from invite_org_session")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for invite_org_session")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q inviteOrgSessionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no inviteOrgSessionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from invite_org_session")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for invite_org_session")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o InviteOrgSessionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(inviteOrgSessionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inviteOrgSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `invite_org_session` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, inviteOrgSessionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from inviteOrgSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for invite_org_session")
	}

	if len(inviteOrgSessionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *InviteOrgSession) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindInviteOrgSession(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *InviteOrgSessionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := InviteOrgSessionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inviteOrgSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `invite_org_session`.* FROM `invite_org_session` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, inviteOrgSessionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in InviteOrgSessionSlice")
	}

	*o = slice

	return nil
}

// InviteOrgSessionExists checks if the InviteOrgSession row exists.
func InviteOrgSessionExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `invite_org_session` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if invite_org_session exists")
	}

	return exists, nil
}

// Exists checks if the InviteOrgSession row exists.
func (o *InviteOrgSession) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return InviteOrgSessionExists(ctx, exec, o.ID)
}
